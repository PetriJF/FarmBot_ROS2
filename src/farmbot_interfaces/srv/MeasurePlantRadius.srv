int64 index                         # Plant index in the active map
---
bool success
string message
float32 plant_radius                # mm, max distance from frame centre plus padding
bool no_plant_detected              # true only when the frame held no qualifying green pixels
